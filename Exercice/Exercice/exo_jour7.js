function filArray(num) {
    var arr = [];
    var addElement = [];
    for (var i = 0; i < 12; i++){
        var dialogue= prompt();
        arr.push(i);
    }
    var addElement = [];
    return arr;
}